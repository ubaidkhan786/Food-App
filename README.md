# Food-App
Developed a web application using Django that allows users to manage food items through CRUD operations. Utilized Django's models to design the database schema, defining attributes like name, description, price, and category for food items.
